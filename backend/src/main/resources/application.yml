spring:
  application:
    name: family-goals-api
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/family_goals}
    username: ${DB_USERNAME:family_goals}
    password: ${DB_PASSWORD:family_goals}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
server:
  port: 8080
